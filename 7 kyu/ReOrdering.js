//There is a sentence which has a mistake in its ordering.

//The part with a capital letter should be the first word.

//Please build a function for re-ordering

//text.split(' ').map((ele)=> /^[A-Z]/.test(ele)) check if contain capital letters or not 


function reOrdering(text){
    // const newArr = []
    // const arr = text.split(' ')
    // for (let i = 0; i < arr.length; i++) {
    //     arr[i][0] === arr[i][0].toUpperCase() ?  newArr.unshift(arr[i]) : newArr.push(arr[i])
    // }
    // return newArr.join(' ')

  }


  console.log(reOrdering('wario LoBan hello'))